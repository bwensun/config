server:
  port: 10080

spring:
  application:
    name: config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bwensun/config.git
          default-label: master
          #配置文件所在根目录
          search-paths: test
        bootstrap: true
